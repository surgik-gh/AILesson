# ✅ Проблема решена!

## Что было не так

В файле `vercel.json` были ссылки на секреты Vercel (с символом `@`):

```json
"env": {
  "DATABASE_URL": "@database-url",  // ❌ Это ссылка на секрет
  "NEXTAUTH_URL": "@nextauth-url",
  ...
}
```

Vercel пытался найти эти секреты, но они не существовали.

## Что я исправил

Удалил секцию `env` из `vercel.json`. Теперь файл выглядит так:

```json
{
  "buildCommand": "prisma generate && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
```

## Изменения запушены

Изменения уже отправлены в GitHub. Vercel автоматически начнёт новый деплой.

---

## 🎯 Следующие шаги

### 1. Добавьте переменные окружения в Vercel Dashboard

Откройте: https://vercel.com/dashboard → ваш проект → Settings → Environment Variables

Добавьте следующие переменные:

```bash
# База данных (Supabase)
DATABASE_URL=postgres://postgres.pnhmrddjsoyatqwvkgvr:f19p3gtv6E1owOUf@aws-1-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true

# NextAuth
NEXTAUTH_SECRET=3ivwX0knmak41v6l7asbqmkHkkpXUZkdWOOL92CO0HQ=
NEXTAUTH_URL=https://your-app.vercel.app

# AI Services (получите ключи)
GROQ_API_KEY=gsk_ваш_ключ
OPENROUTER_API_KEY=sk-or-v1-ваш_ключ

# Настройки
CHAT_COST_PER_MESSAGE=5
MAX_FILE_SIZE_MB=50
ALLOWED_FILE_TYPES=pdf,png,jpg,jpeg,txt
```

**Важно:** Для каждой переменной выберите все окружения:
- ✅ Production
- ✅ Preview  
- ✅ Development

### 2. Дождитесь завершения деплоя

Vercel автоматически начнёт новый деплой после push. Проверьте статус в:
https://vercel.com/dashboard → ваш проект → Deployments

### 3. Обновите NEXTAUTH_URL

После успешного деплоя:
1. Скопируйте URL вашего приложения
2. Settings → Environment Variables
3. Найдите `NEXTAUTH_URL` → Edit
4. Вставьте реальный URL
5. Save

### 4. Redeploy

После обновления `NEXTAUTH_URL`:
- Deployments → последний деплой → три точки → Redeploy

### 5. Запустите миграции

На вашем компьютере:

```bash
cd ailesson-platform

# Войдите в Vercel (если еще не вошли)
vercel login

# Подключитесь к проекту
vercel link

# Загрузите переменные окружения
vercel env pull .env.production

# Запустите миграции
npx prisma migrate deploy

# Заполните базу данных
npx prisma db seed
```

---

## ✅ Готово!

После выполнения всех шагов ваше приложение будет работать!

---

## 📚 Дополнительные ресурсы

- **Получение API ключей:** `ГДЕ_ВЗЯТЬ_КЛЮЧИ.md`
- **Быстрый старт:** `QUICK_START_RU.md`
- **Полная инструкция:** `VERCEL_DEPLOYMENT_RU.md`
